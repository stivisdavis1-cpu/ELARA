[tool.poetry]
name = "api-ai"
version = "0.1.0"
description = "FastAPI AI service for ELARA"
authors = ["ELARA Team"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.111.0"
uvicorn = "^0.30.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
