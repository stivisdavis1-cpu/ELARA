apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-api-ai
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: api-ai
